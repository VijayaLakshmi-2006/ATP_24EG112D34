# Week 1 Assignments - JavaScript Basics

This folder contains JavaScript programs that help in learning the basic concepts of programming. Each file focuses on a specific task.

## Assignments List

### Day 1: Basic Logic and Math
* **day1 ass1.js**: Finds which number is bigger between two numbers.  
* **day1 ass2.js**: Finds the biggest number among three different numbers.  
* **day1 ass3.js**: Adds up all the numbers in a list (array) and shows the total sum.  
* **day1 ass4.js**: Finds the smallest number in a list (array).  
* **day1 ass5.js**: A reusable function that takes three numbers and tells you which one is the biggest.  
* **day1 ass6.js**: A reusable function that takes a list of numbers and returns their total sum.  
* **day1 ass7.js**: A reusable function that looks for a specific number in a list and tells you where it is located (its position).  

### Day 2: Working with Data (Objects and Arrays)
* **day2ass1.js**: Shows how to manage a list of employees. It includes:  
  * Adding a new employee to the list.  
  * Removing an employee by their name.  
  * Changing specific details (like marks) for an employee.  

## Concepts Learned

1. **Variables**: Using `let` and `const` to store information like numbers and names.  
2. **Conditions (If/Else)**: Making the computer choose between different options based on rules.  
3. **Loops (For Loop)**: Telling the computer to do the same task many times, like looking through every item in a list.  
4. **Arrays**: Storing a group of items (like a list of marks or numbers) in a single place.  
5. **Functions**: Writing a block of code once and using it many times with different inputs.  
6. **Objects**: Storing detailed information about a single thing (like an employee with a name, ID, and marks).  
7. **Array Methods**: Using special built-in tools like `splice`, `find`, and `findIndex` to easily change or search through lists.

   #  Week 1 - JavaScript Fundamentals (MERN Stack Preparation)

This repository contains basic JavaScript programs focused on building a strong foundation for the MERN stack. These exercises cover core concepts like functions, arrays, and conditional logic.


##  Files Included

### 1. `arraySum.js`

* Calculates the sum of elements in an array.
* Demonstrates basic array traversal using loops.

### 2. `arrSumUsingFun.js`

* Calculates the sum of array elements using a function.
* Focuses on modular and reusable code.

### 3. `sum Using Function.js`

* Performs addition using a custom function.
* Introduces function parameters and return values.

### 4. `bigInTwo.js`

* Finds the largest number between two values.
* Uses conditional statements (`if-else`).

### 5. `bigInThree.js`

* Finds the largest number among three values.
* Enhances logical comparison skills.

### 6. `smallEle.js`

* Identifies the smallest element in a set or array.
* Demonstrates comparison logic.

---

##  Learning Objectives

* Understand JavaScript basics
* Practice working with arrays
* Learn how to write reusable functions
* Improve problem-solving using conditions
* Build logic for comparisons and calculations

---

##  Tech Used

* JavaScript (ES6 basics)
* Node.js (for running scripts)

---

## How to Run

1. Install Node.js if not already installed
2. Open terminal in project folder
3. Run any file using:

```bash
node filename.js
```

Example:

```bash
node arraySum.js

Move towards:

* Objects & Arrays (advanced)
* DOM Manipulation
* Backend basics with Express.js


This is part of Week 1 practice for building a strong base before diving into full MERN stack development.
